import React from 'react';
import css from "./Home.module.css"

const HomePage = () => {
    return (
        <div className={css.Home}>
            home
        </div>
    );
};

export default HomePage;